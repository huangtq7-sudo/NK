# Client feature modules

Feature code is introduced by vertical slice under this directory. Every feature must keep separate `Model`, `Controller`, and `View` assemblies and follow the dependency direction documented in `NARAKA_开发规范.md`.

Planned modules: Account, Lobby, World, Combat, Character, Weapon, AI, Navigation, Inventory, Quest, Economy, Pet, Presence, and RedDot.
